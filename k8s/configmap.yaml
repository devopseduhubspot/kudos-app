apiVersion: v1
kind: ConfigMap
metadata:
  name: kudos-app-config
  labels:
    app: kudos-app
    component: configuration
data:
  # Frontend environment variables - empty string so /api/kudos works directly
  VITE_API_URL: ""
  
  # Backend environment variables  
  PORT: "3001"
  NODE_ENV: "production"